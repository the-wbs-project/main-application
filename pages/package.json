{
  "name": "pages",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "npx wrangler pages dev ./public --port 4200",
    "deploy": "wrangler pages deploy ./public --project-name wbs-homepage --branch main"
  },
  "devDependencies": {}
}